## Quizz: Fitting a Model

### Multiple Choice
1 point possible (graded)

You can use decision trees for?

* ( ) regression
* ( ) classification
* (x) classification and regression
* ( ) data normalization

---

### Checkboxes
1 point possible (graded)

The following lines of code: `val Array(trainingData, testData) = data.randomSplit(Array(0.7, 0.3))`

* [x] **split the data into training and testing data**
* [ ] train the model
* [x] use 70% of the data for testing
* [ ] use 30% of the data for training
* [ ] make a prediction

---

### Multiple Choice
1 point possible (graded)

In the Random Forest Classifier constructor, `.setNumTrees()`?

* ( ) sets the max depth of trees
* ( ) sets the minimum number of classes before a split
* (x) set the number of trees

---

### Multiple Choice
1 point possible (graded)

Elastic net regularization uses?

* ( ) L0-norm
* ( ) L1-norm
* ( ) L2-norm
* (x) a convex combination of the L1 norm and L2 norm
